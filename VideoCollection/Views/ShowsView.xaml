<UserControl x:Class="VideoCollection.Views.ShowsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:VideoCollection.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" Background="{StaticResource BackgroundBrush}">

    <DockPanel>
        <ScrollViewer DockPanel.Dock="Bottom">
            <StackPanel>
                <Button Margin="0,10,0,0" Height="20" Background="{x:Null}" Foreground="White" BorderBrush="{x:Null}">Add Category</Button>
            </StackPanel>
        </ScrollViewer>
    </DockPanel>
</UserControl>
