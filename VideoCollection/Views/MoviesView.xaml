<UserControl x:Class="VideoCollection.Views.MoviesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:VideoCollection.Views"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" Background="{StaticResource BackgroundBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>

        <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="icCategoryDisplay">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Grid Height="188" Margin="0,0,0,30">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="37.5"/>
                                <ColumnDefinition Width="32"/>
                                <ColumnDefinition Width="1*"/>
                                <ColumnDefinition Width="32"/>
                                <ColumnDefinition Width="37.5"/>
                            </Grid.ColumnDefinitions>

                            <Grid.RowDefinitions>
                                <RowDefinition Height="35"/>
                                <RowDefinition Height="1*"/>
                            </Grid.RowDefinitions>


                            <StackPanel Orientation="Horizontal" Grid.Column="2" Grid.Row="0">
                                <Label Foreground="White" FontSize="18" Content="{Binding Name}" FontWeight="Bold"/>
                                <materialDesign:PopupBox Foreground="White">
                                    <StackPanel>
                                        <Button x:Name="btnRenameCategory" Click="btnRenameCategory_Click" Tag="{Binding Id}">RENAME</Button>
                                        <Button x:Name="btnDeleteCategory" Click="btnDeleteCategory_Click" Tag="{Binding Id}">DELETE</Button>
                                    </StackPanel>
                                </materialDesign:PopupBox>
                            </StackPanel>
                            <Image Grid.Column="1" Grid.Row="1" Cursor="Hand" MouseUp="Back_MouseUp" Source="pack://application:,,,/Images/left_arrow.png" Height="32"/>
                            <ScrollViewer Grid.Column="2" Grid.Row="1" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Disabled">
                                <ItemsControl ItemsSource="{Binding Movies}">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <StackPanel Orientation="Horizontal"/>
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Grid>
                                                <Image Width="272" Height="153" Margin="2,0,2,0" Source="{Binding Thumbnail}" Cursor="Hand">
                                                    <Image.InputBindings>
                                                        <MouseBinding Gesture="LeftClick" Command="{
                                                                                            Binding RelativeSource={
                                                                                                RelativeSource FindAncestor, 
                                                                                                AncestorType={x:Type UserControl}},
                                                                                            Path=DataContext.MovieClickCommand}" CommandParameter="{Binding Id}"/>
                                                    </Image.InputBindings>
                                                </Image>
                                                <materialDesign:PopupBox Foreground="White" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,5,-5,0">
                                                    <StackPanel>
                                                        <TextBlock Visibility="Collapsed" x:Name="hiddenText">
                                                            <TextBlock.Text>
                                                                <MultiBinding StringFormat="{}{0},{1}">
                                                                    <Binding RelativeSource="{RelativeSource FindAncestor, AncestorType={x:Type ItemsControl}}" Path="DataContext.Id"/>
                                                                    <Binding Path="Id"/>
                                                                </MultiBinding>
                                                            </TextBlock.Text>
                                                        </TextBlock>
                                                        <Button x:Name="btnRemoveMovieFromCategory" Click="btnRemoveMovieFromCategory_Click" Tag="{Binding Text, ElementName=hiddenText}">REMOVE FROM CATEGORY</Button>
                                                    </StackPanel>
                                                </materialDesign:PopupBox>
                                            </Grid>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </ScrollViewer>
                            <Image x:Name="imgNext" Grid.Column="3" Grid.Row="1" Cursor="Hand" MouseUp="Next_MouseUp" Source="pack://application:,,,/Images/right_arrow.png" Height="32"/>
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
        <Button Style="{StaticResource MaterialDesignFloatingActionDarkButton}" HorizontalAlignment="Left" VerticalAlignment="Bottom" Margin="8,0,0,8">
            <materialDesign:PopupBox PlacementMode="TopAndAlignLeftEdges" Width="56" Height="56" HorizontalAlignment="Center" VerticalAlignment="Center" PopupVerticalOffset="15">
                <materialDesign:PopupBox.ToggleContent>
                    <Border Width="56" Height="56" HorizontalAlignment="Center" VerticalAlignment="Center">
                        <materialDesign:PackIcon Kind="Plus" Foreground="White" Width="40" Height="40" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                </materialDesign:PopupBox.ToggleContent>
                <StackPanel>
                    <Button x:Name="btnNewMovie" Click="btnNewMovie_Click">NEW MOVIE</Button>
                    <Button x:Name="btnUpdateExistingMovie" Click="btnUpdateExistingMovie_Click">UPDATE MOVIE</Button>
                    <Button x:Name="btnAddCategory" Click="btnAddCategory_Click">ADD CATEGORY</Button>
                </StackPanel>
            </materialDesign:PopupBox>
        </Button>
    </Grid>
</UserControl>
